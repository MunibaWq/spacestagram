import React from 'react';
import LoadingIcon from '../assets/loading.svg';

const Loading = () => {
  return (
    <div>
      <img src={LoadingIcon} alt='loading' />
    </div>
  );
};

export { Loading };
